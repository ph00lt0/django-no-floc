============
Installation
============

At the command line::

    $ easy_install django-no-floc

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-no-floc
    $ pip install django-no-floc
